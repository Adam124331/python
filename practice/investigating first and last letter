strings = ['foo', 'bar', 'baz', 'qux', 'quux','fdsdf']
count = 0

for string in strings:
    if string[0] == string[-1]:
        count += 1

print('Number of strings with first and last character same:', count)

OUTPUT:
Number of strings with first and last character same: 1
